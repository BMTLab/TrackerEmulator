<?xml version="1.0" encoding="UTF-8" ?>

<Frame
    x:Class="TrackerClientEmulator.Views.NavigationItem"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Name="TFrame"
    Margin="0,0,0,4"
    Padding="2"
    x:FieldModifier="public"
    BackgroundColor="{StaticResource LightBackgroundColor}"
    CornerRadius="4"
    HeightRequest="55"
    Opacity="0.95">
    <Label
        x:Name="TLabel"
        BackgroundColor="{Binding Source={x:Reference TFrame}}"
        FontSize="Medium"
        HorizontalTextAlignment="Center"
        Text="{Binding Title}"
        TextColor="{StaticResource LightTextColor}"
        VerticalTextAlignment="Center" />
    <Frame.GestureRecognizers>
        <TapGestureRecognizer
            Command="{Binding Command}"
            CommandParameter="{Binding CommandParameter}"
            Tapped="TapGestureRecognizer_OnTapped" />
    </Frame.GestureRecognizers>
</Frame>

<!--<TapGestureRecognizer Command="{Binding Command, Source={x:Reference TFrame}}" CommandParameter="{Binding CommandParameter, Source={x:Reference TFrame}}" />-->
<!--  BackgroundColor="{StaticResource LightBackgroundColor}"  -->